import { RaportBlerjeMockData } from './raport-blerje-mock-data';

describe('RaportBlerjeMockData', () => {
  it('should create an instance', () => {
    expect(new RaportBlerjeMockData()).toBeTruthy();
  });
});
